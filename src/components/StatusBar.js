import React from 'react';
import { StatusBar } from 'expo-status-bar';

const CustomStatusBar = () => {
  return <StatusBar style="dark" backgroundColor="#ffffff" translucent={false} />;
};

export default CustomStatusBar;